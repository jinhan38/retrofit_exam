import 'package:flutter/material.dart';
class PromotionMain extends StatelessWidget {
  const PromotionMain({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
